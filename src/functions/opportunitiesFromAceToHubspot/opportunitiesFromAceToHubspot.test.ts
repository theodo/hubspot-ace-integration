//import { createOpportunityObject } from "./handler";

describe("reading opportunity", () => {
  test("should create an inbound opportunity that follows the model of ACE accepted opportunities", () => {});
});
